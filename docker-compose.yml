services:
  qdrant:
    image: qdrant/qdrant:latest
    ports:
      - "6333:6333"   # REST API
      - "6334:6334"   # gRPC API (used by the .NET client)
    volumes:
      - qdrant_storage:/qdrant/storage

volumes:
  qdrant_storage:
